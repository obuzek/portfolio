# GitHub token for fetching pinned repositories
# Create a fine-grained token at: https://github.com/settings/tokens?type=beta
# Required scope: Public Repositories (read-only)
GITHUB_TOKEN=your_token_here
