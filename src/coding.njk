---
layout: layouts/base.njk
title: Coding
---

<div class="page-header">
  <h1>Coding Projects</h1>
  <p class="subtitle">Open source work and personal projects</p>
</div>

<div class="items-list">
  {% for repo in github.repos %}
  <article class="item-card">
    <h3><a href="{{ repo.url }}" target="_blank" rel="noopener">{{ repo.title }}</a></h3>
    {% if repo.language %}<p class="item-meta">{{ repo.language }}</p>{% endif %}
    <p class="item-description">{{ repo.description }}</p>
    {% if repo.topics.length > 0 %}
    <div class="item-tags">
      {% for topic in repo.topics %}
      <span class="tag">{{ topic }}</span>
      {% endfor %}
    </div>
    {% endif %}
  </article>
  {% else %}
  <p>No coding projects listed yet. Check back soon!</p>
  {% endfor %}
</div>
